《琉璃泡泡》协助精灵系统设计方案（V1.1）

一、系统定位

系统名称

协助精灵（Assist Spirit）

系统定位

协助精灵不是宠物，也不是单纯的皮肤。

它们是炮台的递球助手，同时拥有各自的专属能力。每局仅可携带 1 只协助精灵。

专属能力分为三类：

1. 普通递球：只负责递送普通泡泡，不附加能力。
2. 功能球制造：在真实装填链路中把普通泡泡转换为专属功能球。
3. 全局技能：不修改炮台中的球，直接对棋盘目标施放一次全局效果。

玩家升级精灵后不直接增加攻击力，也不提供常驻属性 Buff，而是提高专属能力的触发概率。

因此：

精灵 = 递球角色 + 专属能力施放者

而不是：

精灵 = 常驻属性 Buff 提供者

这种设计能够保留核心泡泡龙数值结构，同时形成长期成长目标。

落地前提

当前项目中已经存在普通递球手表现与炮台装填链路。

协助精灵系统不是推翻现有递球手重新实现，而是在现有普通递球手基础上扩展：

普通递球手 = 默认协助精灵米露

后续新增精灵 = 可配置的递球手角色与专属能力施放者

第一版必须保留现有炮台、发射队列、递球节奏和关卡发射数规则，只扩展递球手身份、动作表现和专属能力入口。

核心原则：

1. 递球手表现只负责显示待机、递球和施法动作。
2. 当前球、下一颗球、剩余发射数、队列推进仍由射手/玩法权威逻辑管理。
3. 功能球转换必须写入真实射手队列，不能只在表现层替换图片。
4. 全局技能结果必须由玩法权威层计算并写入棋盘运行时状态，表现层只能播放已确定的结果。
5. 普通递球手必须作为默认配置存在，没有出战特殊精灵时行为与当前版本一致。
6. 新增精灵、新增动作、新增功能球或全局技能时，应优先增加配置和注册项，不修改核心递球流程。
7. 配置、技能注册、目标规则、资源或动作缺失时必须直接报错，不允许静默跳过、隐藏精灵或改用其他能力。

二、世界观设定

在琉璃大陆，每一只协助精灵都掌握一种独特的元素魔法。

每次玩家完成一次真实发射后，精灵都会负责搬运下一颗泡泡。拥有功能球制造能力的精灵可以在装填时改造泡泡；拥有全局技能的精灵则可以在本次发射结算过程中直接影响棋盘。

功能球制造流程：

普通泡泡

↓

协助精灵抱起泡泡并施法

↓

泡泡变成功能球

↓

递入炮台

↓

玩家发射

全局技能流程：

玩家完成一次真实发射

↓

基础命中与特殊球效果结算

↓

协助精灵施放全局技能

↓

棋盘写入技能结果并完成消除、掉落或状态变化

↓

递入下一颗球并恢复玩家操作

两类能力都必须让玩家清楚感受到效果来自当前出战精灵。

三、局内表现与结算时序

待机

精灵始终漂浮在炮台左侧或右侧。

动作包括：

- 上下缓慢漂浮
- 翅膀轻轻扇动
- 看向炮台
- 偶尔挥手

递球

玩家发射后，精灵飞向备用泡泡，抱起泡泡并送入炮口，然后返回待机状态。

递球动作只消费玩法层已经确定的下一颗球信息，不得生成球、改写球类型或推进发射队列。

功能球制造

当出战精灵的功能球能力触发时，玩法层必须先将真实队列中的普通泡泡转换为功能球，表现层再播放施法、发光、变形和递入炮台动画。

全局技能

全局技能只能在一次真实发射的结算链中触发，推荐统一时序为：

1. 玩家发射被玩法层确认成功。
2. 本次投射物的命中、匹配、特殊球效果和已有棋盘机关完成结算。
3. 检查当前精灵的全局技能是否存在合法目标。
4. 对有合法目标的技能进行一次等级概率判定。
5. 判定成功后，玩法层先生成唯一的技能结算结果，再播放 cast 和专属特效。
6. 技能造成的消除、状态变化、支撑重算和自然掉落全部完成。
7. 检查胜负、同步下一颗球并恢复输入。

一次真实发射最多触发一次协助精灵能力。技能造成的消除、掉落、连锁和动画不得再次触发协助精灵能力，防止递归施法。

技能结算期间必须锁定发射输入。暂停、弹窗和纯动画播放不算真实发射，也不得增加触发次数或推进持续回合。

四、精灵与专属能力

精灵	属性	能力类型	专属能力	作用
米露（Milu）	无属性	普通递球	无	初始伙伴，只执行待机与递球，不触发额外能力
露米（Lumi）	火焰	功能球制造	炸弹球	触发时将待装填的普通泡泡转换为炸弹球，命中后产生小范围爆炸
诺亚（Noya）	风	全局技能	龙卷风	释放龙卷风进入棋盘区域；龙卷风移动时吸附路径周围的可吸附球，停止后所有被吸附球进入自然掉落结算
芙洛（Flora）	森林	全局技能	解除束缚	释放解除束缚魔法，解除棋盘内全部已被藤蔓束缚的球
洛可（Loco）	冰晶	全局技能	融雪	暂时解除施法时棋盘内全部冰冻球的冰冻效果，技能持续结束后，仍留在棋盘上的目标球恢复原冰冻状态
可露（Kelu）	雷电	全局技能	闪电链	对棋盘中的合法目标释放闪电链，被链到的球直接消除
悠米（Yumi）	星光	全局技能	星愿复刻	从当前棋盘可合法释放的其他全局技能中随机选择一个并立即释放

能力分类约束：

1. 米露没有能力判定，行为必须与当前普通递球手一致。
2. 露米只修改真实射手队列中的待装填普通球，不直接修改棋盘。
3. 诺亚、芙洛、洛可、可露和悠米只修改棋盘运行时状态，不得改写当前球、下一颗球或剩余发射数。
4. 悠米只可复刻已注册且声明为可复刻的全局技能，不可复刻米露、露米或自身，不允许递归复刻。

五、全局技能详细规则

5.1 诺亚：龙卷风

目标效果：

1. 龙卷风从棋盘外的配置入口进入，沿配置路径移动并在配置终点停止。
2. 球心首次进入吸附半径时即被记录为本次技能的吸附目标，同一颗球只能吸附一次。
3. 被吸附球立即脱离原棋盘格并跟随龙卷风表现移动，不再参与技能期间的匹配、支撑和碰撞。
4. 龙卷风停止后，全部被吸附球必须复用现有自然掉落链路，参与掉落动画、罐子收集、分数、任务和目标统计。
5. 被吸附球离开棋盘后必须统一重算顶部支撑；由此产生的无支撑球也进入正常自然掉落链路。

目标规则：

- 默认可吸附普通球和明确注册为可移动、可掉落的特殊球。
- 虫洞、藤蔓魔灵等永久棋盘实体以及配置为不可移动的任务实体不可吸附。
- 入口、路径、速度、吸附半径、终点和目标策略都是技能必填配置，缺失或数值非法时直接报错。

5.2 芙洛：解除束缚

目标效果：

1. 技能目标是施法时棋盘内全部已经生效的藤蔓束缚球。
2. 一次性清除这些球的藤蔓归属和束缚状态，球本身、颜色和其它特殊属性保持不变。
3. 技能不伤害、不移除藤蔓魔灵，也不永久阻止藤蔓魔灵后续再次束缚球。
4. 解除完成后立即重算顶部支撑；失去藤蔓锚定后无支撑的球进入自然掉落链路。
5. 技能只能在藤蔓预告和藤蔓写入均已结算完成的稳定棋盘状态执行，禁止与藤蔓写入并发。

合法目标为至少一个已被藤蔓束缚的球。没有束缚球时不进入概率判定，也不播放空技能。

5.3 洛可：融雪

目标效果：

1. 施法瞬间记录棋盘内全部冰冻球及其原始冰冻类型、层数和状态，形成唯一的融雪目标快照。
2. 持续期间，这批目标球临时按普通可命中、可匹配球参与玩法；冰冻视觉切换为融化状态。
3. 持续时间使用“后续真实发射次数”计数，由技能配置的 durationShots 决定。暂停、弹窗和动画不消耗持续次数。
4. 持续期间被消除或掉落的目标球正常离场，技能结束时不得重新生成。
5. 技能结束后，在当前发射完整结算结束且输入恢复前，将仍位于棋盘中的目标球恢复为施法前记录的冰冻类型、层数和状态。
6. 施法后新生成或新进入冰冻状态的球不属于本次快照，不受本次融雪影响。
7. 同一精灵能力一次真实发射最多触发一次；融雪尚未结束时不得重复触发或叠加持续时间。

合法目标为至少一个当前处于冰冻状态且未被其它融雪效果占用的球。durationShots 缺失、不是正整数或恢复快照不完整时必须直接报错。

5.4 可露：闪电链

目标效果：

1. 玩法层根据必填配置确定首个目标、最大链数、链距和后续目标优先级。
2. 每个目标在同一条闪电链中只能命中一次；合法目标少于最大链数时，闪电链在最后一个合法目标处自然结束。
3. 被闪电链命中的球按“技能直接消除”结算，不要求同色匹配。
4. 全部链击完成后统一重算顶部支撑，无支撑球进入自然掉落链路。
5. 闪电链消除与自然掉落必须进入正常分数、任务和目标统计，并标记正确的技能来源。

默认目标为允许被技能直接消除的普通球和已注册特殊球。永久实体、不可消除障碍和未注册目标不得进入闪电链。目标选择必须由玩法权威层完成，表现层不得根据节点距离自行选择目标。

5.5 悠米：星愿复刻

目标效果：

1. 候选池只包含已注册、copyable=true、当前棋盘满足释放前提的其他全局技能。
2. V1.1 候选技能为诺亚的龙卷风、芙洛的解除束缚、洛可的融雪和可露的闪电链。
3. 玩法权威层使用本局受控随机源，按技能配置的 copyWeight 从合法候选池中选择一次。
4. 选中后完整复用目标技能的玩法规则和配置，只把施法者身份与表现资源记录为悠米。
5. 悠米不得选择自身、普通递球能力、功能球制造能力或 copyable=false 的全局技能。
6. 当前棋盘不存在任何合法候选技能时，不进入概率判定，也不播放空技能。

候选池、权重、选择结果或复用技能结算缺失时必须直接报错，不允许静默改为固定技能。

六、成长系统

成长不增加攻击力，也不直接强化技能范围、目标数量、持续回合或伤害。

成长统一影响“专属能力触发概率”：

- 功能球制造型：提高普通泡泡被转换为专属功能球的概率。
- 全局技能型：提高一次真实发射结算中施放全局技能的概率。
- 普通递球型：无能力概率，升级只用于外观或后续系统，不得误触发技能。

示例概率曲线：

等级	专属能力触发概率
Lv1	3%
Lv2	5%
Lv3	7%
Lv5	10%
Lv10	18%
Lv20	30%

统一判定规则：

1. 每次真实发射最多获得一次判定机会。
2. 必须先验证该能力当前存在合法触发条件，再执行概率判定。
3. 概率判定必须使用玩法权威层的受控随机源，结果写入 runtime event 或 runtime snapshot。
4. 技能动画、掉落、连锁、剩余球奖励和非真实发射行为不得触发概率判定。
5. 概率曲线缺失、等级越界、随机结果缺失或同一次发射重复判定时必须直接报错。

升级收益为：

等级越高 → 专属能力在满足释放条件时越容易触发。

七、升级材料

升级消耗：

- 金币
- 对应精灵碎片

碎片来源：

- 关卡奖励
- 每日挑战
- 活动奖励
- 商城兑换
- 重复获得精灵自动转化碎片

八、养成结构

建议采用简单成长模式：

精灵

↓

等级提升

↓

专属能力触发概率提升

↓

继续升级

V1.1 不增加技能树、多分支加点和复杂天赋。技能范围、链击数量、融雪持续回合等玩法参数统一由静态技能配置控制，不由 UI 自行计算。

九、UI 结构

精灵大厅

顶部显示“协助精灵”，中央显示当前出战精灵，下面显示所有精灵卡片。

当前出战卡必须显示：

- 精灵名称、等级和星级
- 能力类型
- 专属能力名称
- 当前触发概率
- 更换入口

示例：

当前出战

诺亚 Lv10 ★★★★★

全局技能：龙卷风

当前触发概率：18%

【更换】

精灵详情必须显示：

- 精灵大立绘
- 精灵名称、等级和星级
- 能力类型与完整效果说明
- 当前等级触发概率
- 下一等级触发概率
- 升级消耗
- 【升级】和【出战】按钮

界面信息应突出“升级提高触发概率”，不能把全局技能错误描述为功能球。概率来自统一配置，不允许 UI 自行推导。

十、局内 UI 与反馈

炮台旁边始终显示当前协助精灵。

功能球制造触发时，玩家应看到“普通泡泡 → 精灵施法 → 功能球 → 进入炮台”的完整因果链。

全局技能触发时，玩家应看到：

当前精灵进入 cast 状态

↓

显示精灵名称与技能名称

↓

播放棋盘技能特效

↓

球的消除、掉落或状态变化与玩法结果同步

融雪等持续技能需要显示剩余真实发射次数。技能结束并恢复冰冻时必须有明确恢复反馈，不能只修改逻辑状态。

技能未触发时不播放施法特效；没有合法目标时不显示“触发失败”动画。

十一、阶段性落地任务

阶段一：递球手表现基础化

目标：

在现有普通递球手基础上建立可扩展的协助精灵表现结构。

范围：

1. 保留现有普通递球手，定义为默认协助精灵米露。
2. 局内只实现 idle 和 deliver 两个动作状态。
3. 递球手始终显示在炮台旁边。
4. 玩家发射后，递球手执行递球动作，将玩法层已确定的下一颗球送入炮台。
5. 不接入精灵升级、概率、施法、棋盘技能和精灵大厅。

技术要求：

1. 配置必须包含 spiritId、displayName、prefabPath 或 animationPath、position、idleAction 和 deliverAction。
2. 表现状态机至少支持 idle、deliver，结构上允许后续注册 cast、enter、exit、celebrate。
3. 表现层不得生成球、改写球类型、扣减发射数或推进队列。
4. 默认普通递球手缺配置、缺资源或缺动作时必须直接报错。

验收标准：

1. 进入局内后能看到米露待机。
2. 每次成功发射后播放递球动作。
3. 当前球、下一颗球、剩余发射数与原有逻辑完全一致。
4. 没有任何能力概率变化和额外兜底逻辑。

阶段二：协助精灵配置与出战结构

目标：

把单个默认递球手扩展为可选择、可注册能力的协助精灵配置。

范围：

1. 建立协助精灵静态配置表和能力注册表。
2. 支持当前出战精灵 spiritId。
3. 根据出战精灵加载对应待机、递球和预留施法资源。
4. 暂不开放完整精灵大厅，可先通过固定配置或正式玩家状态字段指定出战精灵。

技术要求：

1. 所有精灵配置必须声明 spiritId、displayName、elementType、abilityKind、abilityId、prefabPath 或 animationPath、idleAction、deliverAction 和 position。
2. abilityKind 只能是 none、produced_ball 或 global_skill。
3. produced_ball 必须关联已注册 producedBallType；global_skill 必须关联已注册 skillId 和 castAction；none 不得携带功能球或全局技能配置。
4. 配置字段必须按 abilityKind 严格互斥校验，缺字段、多余冲突字段、重复 id 或出战精灵不存在时直接报错。
5. 米露配置必须作为唯一默认项存在。

验收标准：

1. 切换不同出战精灵后，局内待机和递球表现跟随变化。
2. 不影响射手队列和发射规则。
3. 新增精灵只需增加配置、注册项和资源，不修改递球主流程。

阶段三：专属能力权威触发框架

目标：

建立功能球制造和全局技能共用的单次发射触发入口。

范围：

1. 在玩法权威层记录真实发射序号和本次能力判定状态。
2. 接入等级概率曲线、合法目标检查和受控随机结果。
3. 露米触发后将待装填普通球转换为真实炸弹球。
4. 全局技能先只产生确定的技能事件，不实现具体棋盘效果和完整动画。

技术要求：

1. 一次真实发射只能判定一次且最多触发一个能力。
2. 功能球转换结果必须写入 ShooterController 权威队列。
3. 全局技能结果必须绑定发射序号、spiritId、skillId 和确定的目标结果。
4. 表现层只能消费结果，不能自行随机、选择目标或改写队列。
5. 概率、等级、能力注册或发射序号非法时直接报错。

验收标准：

1. 露米当前球显示与实际发射类型一致。
2. 全局技能事件不会重复、跨发射串扰或递归触发。
3. 米露行为与当前版本一致。
4. 各能力触发概率随等级配置变化。

阶段四：五种全局技能玩法实现

目标：

按本设计第五节实现诺亚、芙洛、洛可、可露和悠米的权威玩法结果。

范围：

1. 诺亚：路径吸附、脱离棋盘、停止后自然掉落、支撑重算。
2. 芙洛：全盘解除藤蔓束缚、保留藤蔓魔灵、支撑重算。
3. 洛可：目标快照、临时融雪、真实发射次数计时、到期恢复。
4. 可露：权威目标选择、闪电链直接消除、支撑重算。
5. 悠米：合法候选池、受控随机、复用所选技能规则、禁止递归。

技术要求：

1. 每个技能使用独立 handler 注册到统一全局技能注册表，禁止在核心结算流程堆叠 spiritId 条件分支。
2. handler 必须明确 validateTargets、resolveResult、applyResult 和 serializeRuntimeState 合同。
3. 消除、掉落、目标和得分必须复用现有权威结算系统，禁止只删渲染节点。
4. 技能执行期间锁定输入，结算完成后再进行胜负判断和恢复输入。
5. 非法目标、重复目标、未注册实体、融雪快照不一致或复刻结果不完整时直接报错。

验收标准：

1. 五种技能的玩法结果与第五节逐项一致。
2. 自然掉落、直接消除、解除束缚和临时融雪在 runtime snapshot 中可准确还原。
3. 技能结束后没有残留输入锁、幽灵节点或过期状态。
4. 技能结果可通过确定性测试复现。

阶段五：施法与棋盘特效表现

目标：

让玩家明确看到“当前精灵施法 → 棋盘产生结果”的完整因果链。

范围：

1. 增加 cast 状态及五种全局技能专属表现。
2. 诺亚显示龙卷风路径、吸附过程和释放掉落。
3. 芙洛显示全盘藤蔓断裂或消散。
4. 洛可显示融化、持续状态和到期重新冻结。
5. 可露显示与权威目标顺序一致的闪电链。
6. 悠米先显示星光复刻，再播放被选中技能的效果表现。

技术要求：

1. 玩法层必须先确定结果，表现层再按结果播放动画。
2. 动画结束只推进表现状态，不得再次随机、选择目标或应用技能。
3. 表现目标顺序必须与玩法结果一致。
4. cast、技能特效或持续状态资源缺失时直接报错。

验收标准：

1. 动画目标、顺序和最终棋盘状态完全一致。
2. 无合法目标或未触发时不播放施法链。
3. 默认米露不播放额外施法动作。

阶段六：精灵养成与大厅

目标：

提供长期养成入口，让玩家查看、升级和切换协助精灵。

范围：

1. 精灵大厅和详情。
2. 精灵拥有状态、等级、碎片和金币消耗。
3. 出战切换与持久化。
4. 能力类型、完整技能说明、当前概率和下一等级概率展示。

技术要求：

1. 等级概率和技能说明来自统一配置，不允许 UI 自行计算或维护另一份文案映射。
2. 升级材料不足必须明确提示，不允许静默失败。
3. 出战状态和精灵成长状态必须持久化并进入玩家档案同步范围。
4. UI 重复列表必须遵守项目 Sprite 代理分层规则。
5. 列表缺节点、Sprite、SpriteFrame、尺寸或层级非法时直接报错。

验收标准：

1. 玩家可以查看所有精灵及其正确能力类型。
2. 玩家可以升级已拥有精灵并看到概率变化。
3. 玩家可以切换出战精灵。
4. 局内角色、能力类型和触发概率跟随出战精灵变化。

十二、后续扩展

未来可以同时扩展新的功能球制造型精灵和全局技能型精灵。

示例：

新精灵	能力类型	专属能力
奥瑞（Ori）	功能球制造	穿透球
艾拉（Aira）	功能球制造	彩虹球
希诺（Shino）	全局技能	时间停滞
莫莫（Momo）	全局技能	范围吸附
莱恩（Lain）	全局技能	镜像复制

新增能力时只新增精灵配置、能力注册、权威 handler、表现 handler 和资源，不修改已有能力及核心递球流程。

十三、系统优势

与核心玩法深度绑定：精灵既参与递球，也能制造功能球或直接影响棋盘，不是独立宠物系统。

成长反馈直观：升级统一提高专属能力触发概率，功能球型和全局技能型的收益口径一致。

表现力强：递球、施法、全局棋盘变化形成完整动画链，增强陪伴感和角色存在感。

数值易于平衡：V1.1 成长只影响触发概率，技能强度由统一静态配置控制，不直接叠加攻击属性。

扩展成本低：能力按类型、注册表和 handler 扩展，不需要重构射手队列或核心递球流程。

十四、后续版本建议（V2.0）

在 V1.1 验证玩法后，可以增加两项轻度成长内容：

1. 精灵皮肤：仅改变外观、递球和施法特效，不影响数值。
2. 能力品质：高等级后小幅强化能力参数，例如炸弹范围、龙卷风吸附半径、融雪持续回合或闪电链目标数。该能力必须作为 V2.0 独立配置和数值系统实现，不能在 V1.1 中提前硬编码。

这样既保持系统简单易懂，也为后续版本和活动运营预留扩展空间。
