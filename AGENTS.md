# AGENTS.md

## 严格模式总原则

本项目使用 Fail-Fast 严格模式。

接手任务时，优先查看根目录 `PROJECT_STRUCTURE.md`，先理解项目模块边界、主入口、运行链路和校验入口，再阅读具体上下文代码。

优先级：
1. 业务正确性
2. 可维护性
3. 可测试性
4. 可运行性

目标：
- 优先暴露问题，而不是隐藏问题
- 优先保证业务正确性，而不是表面可运行
- 禁止为了“看起来更健壮”而添加兜底逻辑

## 禁止行为

除非任务明确要求，否则禁止：

- 写 fallback / 兜底逻辑
- 写默认值兜底
- 写静默失败逻辑
- 写 try-catch 吞异常
- 写空判断后直接 return
- 写 mock 数据兜底
- 写兼容旧逻辑的临时分支
- 写“不确定时使用默认配置”
- 写 `|| []`
- 写 `|| ''`
- 写 `?? defaultValue`
- 写 `catch (e) { return ... }`

## 异常处理规则

- 异常必须向上抛出
- 不允许吞异常
- 不允许只打印日志后继续执行
- 不允许返回空数组、空对象、false、0 来掩盖错误
- 发现非法状态时直接抛错

## 数据规则

- 必填字段缺失时直接报错
- 配置缺失时直接报错
- 接口返回结构不符合预期时直接报错
- 类型不匹配时直接报错
- 不允许自动补全缺失数据

## UI 弹窗渲染规则

实现或改造 UI 弹窗时，必须使用 Sprite 代理分层模式降低 draw call，尤其是商城、签到、背包、任务、奖励等包含重复列表项的界面。

要求：
- 保留 item / prefab 的原始嵌套结构作为逻辑层、布局层、点击层和编辑入口。
- 使用通用代理工具创建独立 Sprite 渲染层，将同图集、同材质、同 blend 的 Sprite 代理节点集中渲染。
- 原始节点只作为锚点和交互节点；代理节点负责显示。
- 原始 Sprite 组件必须禁用渲染，禁止源节点和代理节点同时绘制同一图像。
- `cc.Button`、触摸事件、引导定位、业务状态仍挂在原始逻辑节点上，禁止为了合批直接搬走交互节点。
- 普通 `cc.Label` 会打断合批；列表中的高频数字、价格、数量、短文本应优先使用数字 Sprite、BMFont 或独立文本层集中处理。
- 代理层缺节点、缺 Sprite、缺 SpriteFrame、尺寸非法、层级配置错误时必须直接抛错，不允许静默跳过或隐藏。
- 如果界面确实不适合代理模式，必须在修改说明中明确原因和替代 draw call 优化方案。

## 修改代码前必须做

1. 先阅读相关上下文代码
2. 找到真实调用链
3. 明确问题根因
4. 只修改必要代码
5. 不做无关重构
6. 不添加额外兜底
7. 如果本次任务导致项目结构、模块边界、运行入口或主要调用链发生变化，必须同步更新根目录 `PROJECT_STRUCTURE.md`

## 输出要求

每次修改完成后说明：

- 修改了哪些文件
- 为什么这样改
- 是否存在兜底逻辑
- 如果存在，必须说明是用户明确要求的
