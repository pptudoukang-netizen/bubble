# GAMEPLAY_EXPANSION_REQUIREMENTS.md

## 1. 文档目标

本文档整理新一轮局内规则、道具、目标、特殊浮岛关卡和 200 关重做需求，作为后续实现与验收依据。

本需求必须遵守项目 Fail-Fast 严格模式：

- 配置缺失、字段非法、资源缺失、节点结构不符合预期时直接报错。
- 不允许通过默认值、静默返回、mock 数据、临时兼容分支或兜底逻辑掩盖问题。
- 关卡配置、UI 节点、资源路径、道具规则必须显式配置和校验。

## 2. 范围概览

本轮新增或调整内容：

- 新增两种局内广告道具：
  - `three_line_elimination`：消三行。
  - `plus_three_balls`：加三球。
- 新增关卡目标：
  - 收集冰冻雪球。
- 顶部 HUD 目标显示改为按关卡配置动态显示。
- `StartGameView` 的目标预览改为按关卡配置动态显示。
- `HudPanel` 下新增倒计时显示，用于特殊浮岛计时关卡。
- 特殊浮岛关卡改为无限发射球 + 计时模式。
- 重新从头设计 200 关。
- 游戏棋盘上方可延伸到屏幕外，屏幕外顶部球仍参与碰撞、吸附、消除和掉落。

## 3. 新增局内广告道具

### 3.1 通用规则

新增两种道具都属于局内临时广告道具：

- 只能通过观看广告在当前局获得。
- 不进入玩家永久背包。
- 不允许在局前携带。
- 不允许通过商店、签到、宝箱、过关奖励直接发放。
- 获得后只在当前关卡尝试内有效。
- 当前局结束、重开、返回选关或进入下一关后必须清空。
- 广告失败、广告中断、达到频控上限时不得发放。

建议道具 ID：

```json
{
  "three_line_elimination": "消三行",
  "plus_three_balls": "加三球"
}
```

局内底部道具节点已经在 `GameView` 中重新调整，且两种新道具节点已经创建。实现时必须读取现有节点结构并做强校验，缺节点直接报错。

### 3.2 消三行道具

#### 定位

强力局面清理道具，用于快速降低棋盘下压压力，制造掉落和收集机会。

#### 获得方式

- 玩家点击局内底部 `消三行` 道具入口。
- 如果当前局没有可用次数，则触发激励广告。
- 广告成功后，本局获得 `1` 次 `three_line_elimination` 使用次数。
- 获得后可以立即使用，或保留到本局稍后使用。

#### 使用规则

- 只能在局内非终态使用。
- 发射球飞行中、结算动画中、棋盘下压处理中、结果弹窗展示后不得使用。
- 使用后消耗本局 `1` 次 `three_line_elimination`。
- 一次使用必须选择或确定连续三行作为消除目标。

三行选择规则：

- 清除当前棋盘距离危险线最近的连续三行有效球行。
- 如果有效球行不足三行，则只清除现有有效球行。
- 如果当前棋盘没有可清除球，使用入口应不可用并提示当前不可使用。

#### 动画规则

资源路径固定为：

```text
resources/image/light_effect.png
```

使用时表现：

1. 在三条目标行的视觉中心分别生成一条光线。
2. 三条光线从屏幕左侧快速运动到屏幕右侧。
3. 光线运动期间目标行球保持在原位，不提前掉落。
4. 光线动画结束后，目标三行球进入清除/掉落结算。
5. 清除导致失去支撑的球按现有掉落逻辑掉落。
6. 掉落球按现有收集、目标进度、计分、星级逻辑结算。

Fail-Fast 要求：

- `light_effect.png` 缺失时直接报错。
- 目标行坐标无法映射到屏幕空间时直接报错。
- 动画节点创建失败或资源类型不符合预期时直接报错。

#### 结算规则

- 普通颜色球按现有掉落/收集链路处理，不直接视为进罐收集。
- 消三行清除普通颜色球时不提供额外分数，只按照正常规则触发掉落和目标相关结算。
- 消三行直接清除冰冻雪球时，必须计入 `collect_ice_snowball` 目标进度。
- 被清除的技能球不进入炮台。
- 被清除的障碍球不生成普通球，除非实现明确复用破障/解冻语义并同步更新本文档。

以上结算口径必须在实现时写入校验和测试，不允许隐式混用旧逻辑。

### 3.3 加三球道具

#### 定位

资源补给道具，用于在当前局增加容错，不改变棋盘结构。

#### 获得方式

- 玩家点击局内底部 `加三球` 道具入口。
- 如果当前局没有可用次数，则触发激励广告。
- 广告成功后，本局获得 `1` 次 `plus_three_balls` 使用次数。

#### 使用规则

- 只能在局内非终态使用。
- 使用后消耗本局 `1` 次 `plus_three_balls`。
- 当前局剩余发射球数量立即增加 `3`。
- 如果当前关卡是无限发射球计时模式，该道具不应展示或不可使用。

#### UI 反馈

- 使用成功后，底部道具次数扣减。
- 顶部或局内剩余球数量立即刷新。
- 如有飘字/加球动画，应只作为表现，不影响结算时序。

## 4. 新增目标：收集冰冻雪球

### 4.1 目标定义

新增关卡目标：收集冰冻雪球。

建议目标类型：

```json
{
  "type": "collect_ice_snowball",
  "value": 8
}
```

命名说明：

- `collect_ice_snowball` 表示收集冰冻雪球目标。
- 目标对象来自现有冰冻球/雪球实体。
- 具体视觉资源可使用当前已加入的冰冻雪球相关资源，例如 `resources/image/snow_cube.png` 或最终 UI 指定资源。

### 4.2 与现有冰冻球关系

项目已有 `ice` 障碍球，但此前没有收集目标。

本轮需求要求：

- 冰冻球仍按现有特殊实体方式配置。
- 新目标统计冰冻雪球的收集进度。
- 关卡配置必须能表达本关是否需要收集冰冻雪球，以及需要数量。
- 加载时必须校验目标数量与关卡内可收集冰冻雪球供给关系，不允许配置一个无法完成的目标。

### 4.3 计数口径

计数规则：

- 冰冻雪球被消除、掉落并进入收集流程后计数。
- 仅解冻但仍停留在棋盘上的冰冻球不计数。
- 若冰冻球被解冻成普通球后再掉落，只按普通球逻辑处理，不再计为冰冻雪球。
- 若消三行直接清除冰冻雪球，必须立即计入冰冻雪球收集目标。

该口径需要在 `GameManager` 目标进度、runtime snapshot、HUD 渲染和关卡校验中保持一致。

## 5. 顶部 HUD 目标动态显示

### 5.1 目标

顶部目标区域已经重新调整布局。运行时必须读取关卡数据，需要收集哪个目标就显示哪个目标。

### 5.2 显示规则

HUD 不再固定展示某一种收集目标。

应根据 `level.winConditions` 中的目标类型动态展示：

- `collect_any`：显示通用收集目标图标和 `current / target`。
- `collect_color`：显示目标颜色球图标和 `current / target`。
- `collect_ice_snowball`：显示冰冻雪球目标图标和 `current / target`。

如果关卡有多个主目标，必须按配置顺序展示，或按 UI 最终容量定义固定上限。超出 UI 容量时必须报错，不允许静默隐藏。

### 5.3 倒计时

`HudPanel` 下新增倒计时节点，用于特殊浮岛关卡。

显示规则：

- 普通关卡隐藏倒计时。
- 特殊浮岛计时关卡显示倒计时。
- 倒计时必须显示剩余时间。
- 时间归零后，如果目标未完成或尚未点亮至少一颗星，则判负。
- 时间归零前完成目标并点亮至少一颗星，则判胜。

节点要求：

- 运行时必须校验 `HudPanel` 下倒计时节点存在。
- 如果关卡配置为计时模式但节点缺失，直接报错。
- 如果普通关卡显示了倒计时，应视为渲染状态错误并修正为隐藏；节点缺失不应影响普通关卡。

## 6. StartGameView 目标预览

`StartGameView` 中收集目标区域已经调整，开局前必须根据关卡配置展示本关目标。

显示规则与 HUD 保持一致：

- `collect_any`：展示通用收集目标。
- `collect_color`：展示颜色目标。
- `collect_ice_snowball`：展示冰冻雪球目标。
- 特殊浮岛计时关卡需要同时展示计时规则和目标信息。

Fail-Fast 要求：

- 目标类型未知时报错。
- 目标图标资源缺失时报错。
- UI 节点缺失时报错。
- 目标数量不是正整数时报错。

## 7. 特殊浮岛计时关卡

### 7.1 定义

特殊浮岛关卡是无限发射球、计时模式关卡。

默认关卡来源：

- 浮岛地图中每 20 关一个特殊浮岛。
- 特殊关卡 ID 为 `20, 40, 60...`。

### 7.2 配置建议

在关卡配置中新增显式模式字段：

```json
{
  "level": {
    "levelType": "special_floating_island",
    "playMode": "timed_infinite_shots",
    "timeLimitSeconds": 90,
    "shotLimit": null,
    "winConditions": [
      {
        "type": "collect_ice_snowball",
        "value": 8
      },
    ],
    "requiredStarCount": 1
  }
}
```

字段口径：

- `levelType = "special_floating_island"`：表示特殊浮岛关卡。
- `playMode = "timed_infinite_shots"`：表示计时无限球模式。
- `timeLimitSeconds`：倒计时秒数，必须为正整数。
- `shotLimit`：计时无限球模式不使用普通发射上限。最终实现可删除该字段或允许指定特殊值，但必须在文档和加载校验中统一。
- `requiredStarCount`：特殊浮岛计时关要求点亮的星级数量，当前固定为 `1`。
- 星级点亮逻辑复用普通关卡规则，通过 `targetScore`、分数段和现有星级策略计算，不新增 `light_star` 类型。

### 7.3 胜负规则

特殊浮岛计时关卡胜利条件：

- 在倒计时结束前完成本关配置的收集目标。
- 在倒计时结束前点亮至少一颗星。

失败条件：

- 倒计时归零时，任一必要目标未完成。
- 触发其他明确配置的失败条件。

特殊规则：

- 发射球无限，不因剩余球耗尽失败。
- 不显示或不启用 `加三球` 道具。
- 仍允许使用 `消三行`，是否允许其他旧局内道具由关卡 `powerupRules` 显式决定。
- 倒计时暂停/恢复必须跟随游戏暂停、广告播放、后台切前台等生命周期策略；广告播放期间必须暂停倒计时。

## 8. 棋盘上方延伸与屏幕外交互

### 8.1 新棋盘空间规则

重新设计 200 关后，游戏区域上方的球允许延伸到屏幕外。

目的：

- 支持更高、更长的棋盘结构。
- 让下压过程有更大的顶部空间。
- 为特殊浮岛和后期关卡提供纵向变化。
- 每关初始化时必须至少保留 `8` 行下压空间区域。

### 8.2 发射球碰撞与吸附

发射出的球可以与屏幕外顶部球发生碰撞，也允许吸附到屏幕外顶部球形成的新格位。

规则：

- 屏幕外上方的棋盘球可以存在、参与发射碰撞、参与支撑关系。
- 屏幕外顶部球允许作为发射球吸附目标。
- 发射球吸附到屏幕外顶部区域后，仍按普通棋盘球参与后续消除、掉落、目标进度和胜负判断。
- 屏幕外顶部球允许被消除，也允许因失去支撑而掉落。
- 顶部信息栏只影响视觉遮挡和 UI 层级，不应阻止逻辑碰撞、吸附、消除或掉落。
- 发射轨迹、幽灵球和吸附候选点必须使用同一套棋盘坐标规则，不能渲染层和逻辑层各自判断。

### 8.3 渲染要求

- 屏幕外上方的球可以不渲染或被裁切，但其逻辑位置必须存在。
- 进入可视区域后必须正确显示。
- 如果吸附点或消除发生在屏幕外顶部区域，逻辑结算必须正常执行，不需要额外可视提示。
- 顶部信息栏不应遮挡已进入可视区域的吸附反馈、幽灵球和辅助线。

### 8.4 校验要求

- `BoardLayout` 或关卡运行时必须明确棋盘顶部逻辑边界与可视裁切边界。
- `TrajectoryPredictor` 和吸附结算必须使用同一棋盘逻辑边界。
- 渲染层和逻辑层使用的坐标边界不能各自硬编码。
- 关卡初始化必须校验顶部下压空间不少于 `8` 行；不足时直接报错，不允许自动平移或压缩棋盘兜底。

## 9. 200 关重做规则

### 9.1 总目标

从第 1 关开始重新设计 200 关，使关卡、目标、道具、特殊浮岛和地图节奏统一。

### 9.2 关卡节奏

建议结构：

- `1-10`：基础教学，普通收集、颜色收集、反弹和掉落。
- `11-19`：引入障碍与技能球，为第 20 关特殊浮岛做准备。
- `20`：第一个特殊浮岛计时关，教学倒计时、无限球和冰冻雪球目标。
- `21-39`：复合普通关，逐步加入冰冻雪球目标和新道具入口。
- `40`：第二个特殊浮岛计时关。
- 后续每 20 关一个特殊浮岛，持续提升目标数量、结构复杂度和时间压力。

### 9.3 目标类型分布

200 关应覆盖：

- `collect_any`
- `collect_color`
- `collect_ice_snowball`
- 多目标组合
- 特殊浮岛计时目标

目标显示必须始终由关卡配置驱动，不允许 UI 根据关卡 ID 写死。

### 9.4 道具投放节奏

普通关卡：

- 局内广告道具可作为救场入口出现。
- `plus_three_balls` 仅用于有限发射球关卡。
- `three_line_elimination` 可用于普通关和特殊计时关，但必须由关卡规则允许。

特殊浮岛计时关：

- 不启用 `plus_three_balls`。
- 可启用 `three_line_elimination`，用于帮助完成目标和星级。

### 9.5 配置生成与校验

重做 200 关时必须同步更新：

- `levels/level_001.json` 到 `levels/level_200.json`
- `assets/map/config/floating_map.json`
- 关卡校验脚本
- 地图配置生成脚本
- 关卡样例和配置规范文档

所有 200 关必须能通过校验脚本。校验脚本不得跳过缺失关卡或自动补齐配置。

## 10. 关卡配置字段调整建议

### 10.1 主目标字段

继续使用 `winConditions` 表达通关条件，但需要新增类型：

```json
{
  "type": "collect_ice_snowball",
  "value": 8
}
```

特殊浮岛计时关通过特殊关字段表达星级要求，不新增 `light_star` 类型。当前固定要求至少点亮一颗星：

```json
{
  "requiredStarCount": 1
}
```

星级点亮逻辑与普通关卡一致，仍由分数和现有星级策略计算；`requiredStarCount` 固定为 `1`，表示计时关胜利至少需要达到一星分数线。

### 10.2 关卡模式字段

建议新增：

```json
{
  "levelType": "normal",
  "playMode": "shot_limited",
  "timeLimitSeconds": 0
}
```

可选值：

- `levelType`
  - `normal`
  - `special_floating_island`
- `playMode`
  - `shot_limited`
  - `timed_infinite_shots`

普通关：

- `playMode` 必须为 `shot_limited`。
- `shotLimit` 必须为正整数。
- `timeLimitSeconds` 不应配置。

特殊计时关：

- `playMode` 必须为 `timed_infinite_shots`。
- `timeLimitSeconds` 必须为正整数。
- `requiredStarCount` 必须为 `1`。
- `shotLimit` 不应作为失败条件。

### 10.3 局内广告道具规则

建议在关卡配置中显式声明：

```json
{
  "adPowerupRules": {
    "allowed": [
      "three_line_elimination",
      "plus_three_balls"
    ],
    "maxGrantsPerRun": {
      "three_line_elimination": 1,
      "plus_three_balls": 1
    }
  }
}
```

特殊计时关示例：

```json
{
  "adPowerupRules": {
    "allowed": [
      "three_line_elimination"
    ],
    "maxGrantsPerRun": {
      "three_line_elimination": 1
    }
  }
}
```

校验要求：

- `allowed` 中只能出现已定义广告道具 ID。
- `maxGrantsPerRun` 必须覆盖 `allowed` 中所有道具。
- 计时无限球关卡不得允许 `plus_three_balls`。

## 11. 验收标准

### 11.1 道具

- 两种新道具只能通过当前局广告获得。
- 当前局结束后新道具次数清空。
- 消三行动画使用 `resources/image/light_effect.png`。
- 消三行动画结束后才触发三行消除和掉落。
- 消三行清除普通颜色球不提供额外分数。
- 加三球使用后剩余球数增加 `3`。
- 计时无限球关卡不显示或不允许使用加三球。

### 11.2 目标与 UI

- HUD 顶部目标按 `winConditions` 动态显示。
- `StartGameView` 按关卡配置显示目标。
- 冰冻雪球目标可正确计数、刷新、完成。
- 未知目标类型、缺资源、缺节点直接报错。

### 11.3 特殊浮岛计时关

- 特殊浮岛关卡显示倒计时。
- 发射球无限。
- 时间内完成目标并点亮至少一颗星才胜利。
- 时间归零未完成则失败。
- 广告期间倒计时必须暂停且可验证。

### 11.4 棋盘与屏幕外交互

- 棋盘球可存在于屏幕顶部外。
- 屏幕外顶部球参与发射碰撞。
- 发射球允许吸附到屏幕外顶部球形成的新格位。
- 屏幕外顶部球允许被消除和掉落。
- 屏幕外顶部区域的吸附、消除和掉落不需要额外可视提示。
- 每关初始化时至少保留 `8` 行下压空间区域。
- 棋盘顶部逻辑边界和可视裁切边界由统一配置驱动。

### 11.5 200 关

- `1-200` 关文件完整。
- 每 20 关对应一个特殊浮岛关。
- 地图配置覆盖 `1-200`，无重复、无缺失。
- 所有关卡通过配置校验。
